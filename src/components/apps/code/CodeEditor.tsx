import StoreUpdatePhase from './phases/StoreUpdatePhase'

export default function CodeEditor() {
  // Renderiza o StoreUpdatePhase existente (funcionando perfeitamente)
  return <StoreUpdatePhase />
}