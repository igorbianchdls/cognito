declare module '@nivo/legends' {
  // Minimal stub to satisfy type-only imports in the codebase.
  // For full typings, install the real package: `@nivo/legends`.
  export interface LegendProps {
    [key: string]: any;
  }
}

