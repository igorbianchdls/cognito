# ERP server layer

This folder is reserved for API, repositories, Supabase migrations adapters, and tenant-aware business rules.
The first UI iteration uses `frontend/services/erpClient.ts`, which currently points to the mock client.

