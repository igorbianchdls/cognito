interface:
  display_name: "Connected ERP Conta Azul"
  short_description: "ERP Conta Azul via tools do projeto."
  default_prompt: "Use $connected-erp-conta-azul para criar ou consultar registros Conta Azul com dry-run antes de ações reais."

policy:
  allow_implicit_invocation: true
